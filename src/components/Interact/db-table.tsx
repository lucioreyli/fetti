import type { FC } from 'react';
import { Header } from './Header';

export const DBTable: FC = () => {
  return (
    <div className="flex flex-col flex-1 bg-red-000 p-6">
      <Header />
    </div>
  );
};
